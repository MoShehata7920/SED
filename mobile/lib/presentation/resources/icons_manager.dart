import 'package:flutter/material.dart';

class IconsManager {
  static const List<IconData> iconsList = [
    Icons.home,
    Icons.chat,
    Icons.favorite,
    Icons.settings,
  ];

  static const rightArrow = Icon(Icons.keyboard_arrow_right_outlined);

  static const search = Icon(Icons.search);
}
